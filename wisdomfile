Compilers are just translators
Garbage collected programming languages are unusable because when you compile and run your program it deletes itself
"How to become compiler?"
"Trees grow down in CS because programmers never go out of their room to see an actual tree"
"C is a very nice high level programming language" -Unix guy that I forgot his name was
"Google does not want you to use Google as a verb. So I googled if this was true..."
Linus Torvalds always names his projects after himself
mov is Turing complete if you think hard enough
Rust is the safest language ever because with no code comes no bugs
`memcpy` is the best compressor. The worst case is the same as the best case!
`gut status`
/* // Hello, World! */
unsigned i = (-1);
Guess what size ARM64 instructions are
In order to take revenge on the vibe coders we will train AI with the goal of maximizing the loss function
There is zero difficulty in programming. Off-by-one errors
"For more information, send your request to /dev/null"
"The instructions are located in /dev/urandom"
unsigned char main[] = { 0 };
#define ever (;;)
#define true 0
#define false 1
#define private public
#define public private
#define struct union
#define memcpy strncpy
#define malloc(x) ({ (void)malloc(x); errno = ENOMEM; NULL; })
"I just sorted the list and used binary search, what's slow about that?"
`printf` can act like a mutex
Ragebait has existed since 1985 it's called C++
Haskell programmers cannot touch grass, as it may have side effects
C programmers forget what convenience is
Assembly programmers forget what syntax is
Machine code programmers forget what text is
// Behold! Code that works for reasons unexplained!
Linux users after someone said `gcc` is the `GNU C Compiler`:
#define true (!!(__LINE__ & 15)) /* job security */
